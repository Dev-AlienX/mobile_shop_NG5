import { RamFilterPipe } from './ram-filter.pipe';

describe('RamFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RamFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
