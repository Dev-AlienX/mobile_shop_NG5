import { MinMaxPipePipe } from './min-max-pipe.pipe';

describe('MinMaxPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MinMaxPipePipe();
    expect(pipe).toBeTruthy();
  });
});
