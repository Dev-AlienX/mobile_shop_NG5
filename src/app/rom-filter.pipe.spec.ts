import { RomFilterPipe } from './rom-filter.pipe';

describe('RomFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RomFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
